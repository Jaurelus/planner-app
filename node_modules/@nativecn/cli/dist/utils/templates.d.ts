/**
 * Get the template directory for a component
 */
export declare function getTemplateDir(component: string): string;
/**
 * Read component dependencies from dependencies.json if it exists
 */
export declare function getComponentDependencies(component: string): string[];
/**
 * Resolve all dependencies for a component and its nested dependencies
 */
export declare function resolveComponentDependencies(components: string[], resolvedDeps?: Set<string>, processing?: Set<string>): string[];
/**
 * Copy a component template to the user's project
 */
export declare function copyComponentTemplate(component: string, targetDir: string, options?: {
    overwrite?: boolean;
}): Promise<boolean>;
/**
 * Generate an index.ts barrel file that exports all components
 */
export declare function generateComponentIndex(targetDir: string): Promise<void>;
